#ifndef MATRIX_VECT_H
#define MATRIX_VECT_H

#include <grid/grid_struct.h>
#include <matrix_struct.h>

void val_calc(Grid &grd, Matrix &mtr);
void col_calc(Grid &grd, Matrix &mtr);
void poi_calc(Grid &grd, Matrix &mtr);

#endif
